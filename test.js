const mdsm = require('./mdsm')

mdsm.init({
	port: 9001,
	url: '/api/'
});

let endpointURL = 'doSomething1';
let clientClassA = 
let allowedClassTypes = [];
mdsm.addEndpoint({
	url: endpointURL,

});
